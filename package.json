{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "bootstrap": "npx lerna bootstrap",
        "serve": "npx lerna run serve:block --stream --scope"
    }
}
