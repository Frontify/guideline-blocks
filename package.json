{
    "name": "root",
    "private": true,
    "devDependencies": {
        "@frontify/frontify-cli": "^2.0.4",
        "lerna": "^4.0.0"
    },
    "scripts": {
        "bootstrap": "npx lerna bootstrap",
        "serve": "npx lerna run serve:block --stream --scope"
    }
}
